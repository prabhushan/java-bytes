spring:
 application:
    name: serviceB  
 profiles:
    active:
      prod
 cloud:
    config:
      uri: http://localhost:8888
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: refresh